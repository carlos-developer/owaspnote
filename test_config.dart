// Test configuration for OWASP Note project

const testConfig = {
  'integration_test_timeout': Duration(minutes: 3),
  'unit_test_timeout': Duration(minutes: 1),
  'enable_mock_mode': true,
  'clear_data_between_tests': true,
};